using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IssoAi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Isso ai");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
